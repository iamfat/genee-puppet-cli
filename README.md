# genee-puppet-cli

## Installation
```bash
sudo pip install genee-puppet-cli
```

## Usage
```bash
cd /path/to/dir
genee-puppet init

rm -rf conf.d
git clone https://xxx/to/puppet-conf-dir conf.d

genee-puppet run
```
